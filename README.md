# Employee-Attrition-Prediction-using-Random-Forest
This project uses a pre-trained Random Forest Machine Learning model to predict whether an employee is likely to leave the organization or stay.  It is designed as a prediction pipeline, where new employee data is passed through preprocessing and the model to generate actionable insights.
