# Employee-Attrition-Prediction-using-Random-Forest
This project uses a pre-trained Random Forest model to predict whether an employee is likely to leave the organization.  
It is designed as a prediction pipeline, where new employee data is passed through preprocessing and the model to generate actionable insights.
